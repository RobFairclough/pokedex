const React = require('react');

const Divider = props => {
  return (
    <shape id="divider">
      <shape className="hinge" />
      <shape className="hinge" />
      <shape className="hinge" />
    </shape>
  );
};
export default Divider;
