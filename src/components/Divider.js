const React = require('react');

const Divider = props => {
  return (
    <span id="divider">
      <span className="hinge" />
      <span className="hinge" />
      <span className="hinge" />
    </span>
  );
};
export default Divider;
